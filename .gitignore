# ---- Reverse-engineering toolchain (large, re-downloadable) ----
tools/
*.log

# ---- Large intermediate extraction artifacts (byte-exact reproducible from the APK) ----
# Kept locally for provenance/deep-dives, excluded from version control.
# Regenerate with docs/PROVENANCE.md.
original/apk_raw/
original/jadx_full/
original/decoded_res/

# Raw archive payloads inside extracted mini-programs (the unpacked src/ is kept)
original/miniprograms/*/*.tar
original/miniprograms/*/*.amr

# ---- OS / IDE ----
.DS_Store
Thumbs.db
.idea/
*.iml

# ---- Node (mini-program tooling, if used) ----
node_modules/

# ---- Generated Android build artifacts (not source; do not re-add) ----
# jadx emits these from the DEX; they are regenerated by the Android build.
**/R.java
**/BR.java

# ---- Frontend-only Gradle app (buildable reconstruction) ----
.gradle/
build/
app/build/
captures/
local.properties
*.keystore
.cxx/




# Large release APK - uploaded to GitHub manually, kept out of git history
My OneApp.apk
